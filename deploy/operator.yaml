apiVersion: apps/v1
kind: Deployment
metadata:
  name: knopic-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: knopic-operator
  template:
    metadata:
      labels:
        name: knopic-operator
    spec:
      serviceAccountName: knopic-operator
      containers:
        - name: knopic-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - knopic-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "knopic-operator"
